export enum AuthServices {
  apple = "apple",
  linkedIn = "linkedin",
  facebook = "facebook",
  google = "google",
}
export enum UserTypes {
  admin = "admin",
  cook = "cook",
}

export enum Time {
  SECONDS = 1000,
  MINUTES = 60 * SECONDS,
  HOURS = 60 * MINUTES,
  DAYS = 24 * HOURS,
  YEARS = 365 * DAYS,
}
